- @feature_cities.each_with_index do |city, index|
  - last = (index == 3) ? 'last' : ''
  %div{:class => "span-3 #{last}"}
    = link_to city_image_tag(city), city
    %h5= link_to 'Travel guide to '+ city.name, city